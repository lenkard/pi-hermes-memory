# Keep the remote semantic index derived

Curated Memory remains authoritative in its existing human-readable storage, while remote semantic infrastructure is a rebuildable Derived Index. This preserves local writes and lexical retrieval during remote outages, limits infrastructure lock-in, and requires synchronization, reconciliation, and deletion propagation rather than allowing the remote index to own unique memory.
